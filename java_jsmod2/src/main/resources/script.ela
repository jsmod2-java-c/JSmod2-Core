utf-8

a=12
b=13

func test1(a);start:
    a=3;
    # echo(${a});
    return(${a}returned);
    test3(${a}); #这里将
:end

func test2(a);start:
    global::b=2;
    echo(${a});
    return(test3(a));
:end

func test3(a);start:
    a=3;
:end

"""
global::a=0
修改全局变量的值，否则默认是局部变量
"""